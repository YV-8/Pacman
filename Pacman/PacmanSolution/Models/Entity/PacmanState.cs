namespace PacmanSolution.Models;

public enum PacmanState
{
    NORMAL,
    IMBEMCIBLE,
    DIE,
    DIEDING
}
/* cuando este muriendo que se vuelva 0,0 mtodo este comportameinto sera en el model
respanear al pacman es un metodo -> model */